* The script takes a full sentence from the user and counts exactly how many times a specific character appears.
* But to make it smarter, I used Python's built-in string methods
     (.isalpha() and .isdigit()) to dynamically identify if the user searched for a letter, a number, or a special character, customizing the terminal output for each!
