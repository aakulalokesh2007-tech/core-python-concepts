# Here are the core concepts I focused on today:
* ⛓️ Chained Comparisons:
      Instead of writing messy if a > 80 and a <= 90, I used Python's elegant mathematical chaining: if 80 < a <= 90:.
* 🧱 Exception Handling:
      I implemented try/except ValueError blocks so that if a user types text (like "Eighty") instead of a number, the program gracefully catches the error instead of crashing.
* 🔢 Floating Point Math: 
      Swapped integers for float() so the program can accurately grade scores like 89.5%.
