* : An interactive, menu-driven command-line application that allows users to dynamically insert, delete, and sort list elements.
*
*   Demonstrates robust control flow using while loops, conditional routing, and try/except blocks for user input validation.

*   Function: Using eval() is generally discouraged in professional Python because it can run malicious code if the user types something dangerous. It is safer to ask the user to type numbers separated by spaces, and then split them into a list.
