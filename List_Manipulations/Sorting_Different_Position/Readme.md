* Demonstrates an efficient $O(N)$ time complexity solution to repair an array where exactly two elements are out of place, utilizing two-pointer index tracking.

*Add index-tracking swap method for misplaced array elements
