* Add user-input driven list sorting validator
 
 
 * A script that takes dynamic user input to build a list, then uses a while loop and conditional flags to check if the elements are arranged in ascending order.

 * Implement linear traversal to validate list sort order
