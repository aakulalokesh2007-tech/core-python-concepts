*  Demonstrates the "accumulator pattern" and linear scanning to manually calculate the Minimum, Maximum, and Sum of a numerical list from scratch.

*  A fundamental exercise in list iteration and state tracking. This script manually scans a list to identify the maximum and minimum values, and calculates the Sum by accumulating a running total, bypassing Python's built-in statistical functions to demonstrate core algorithmic logic.
