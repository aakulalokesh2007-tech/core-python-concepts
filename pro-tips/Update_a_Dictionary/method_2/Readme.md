# Unpacking ** (The Functional Way) 
* Creating a brand new dictionary using {**default, **user_data}. This is great if you want to strictly avoid mutating your original data, but the syntax can look visually noisy.
